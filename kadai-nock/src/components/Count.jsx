import { useState } from 'react'

function Count(){
    const[count,setCount] = useState(0);
    const addClick = () =>{
        setCount(count +1 );
    }
    const 
    return(
        <div>
            <h1>Count処理</h1>
        </div>
    );
}